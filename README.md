[![Review Assignment Due Date](https://classroom.github.com/assets/deadline-readme-button-22041afd0340ce965d47ae6ef1cefeee28c7c493a6346c4f15d667ab976d596c.svg)](https://classroom.github.com/a/qR-atX5d)
# 🐍 [2026-01] 나만의 파이썬 소프트웨어 개발 프로젝트

## 1. 시나리오 제목
<br>
대학생 맞춤형 '갓생 살기' 투두 메이트(Todo-mate) 시스템



## 2. 시나리오 (5~10줄)
<br>
이 프로그램은 대학생들이 하루의 목표를 설정하고 성취도를 시각적으로 확인할 수 있는 **'자기관리 루틴 시스템'**입니다.<br>
사용자는 자신의 이름, 오늘 날짜, 그리고 계획한 총 할 일의 개수와 실제 완료한 개수를 입력합니다. 시스템은 이를 바탕으로 오늘의 최종 달성률(%)을 계산하여 출력합니다.
단순히 수치만 보여주는 것이 아니라, 달성률 구간에 따라 맞춤형 응원 메시지를 제공하는 기능을 포함합니다. 향후에는 할 일 목록을 직접 배열(List)로 저장하고, 주간 평균 달성률을 산출하거나 카테고리별(공부, 운동 등)로 관리하는 기능을 추가하여 완성도 높은 개인 비서 프로그램을 만들 계획입니다.


## 3. 예상 기능 및 메뉴 (최소 5개)
<br>
1. 사용자 프로필 및 오늘 날짜 설정<br> 
2. 할 일 목록 추가 및 개수 관리<br> 
3. 실시간 달성률(%) 계산 및 출력<br>  
4. 달성 점수에 따른 동기부여 등급 판별<br>  
5. 주간/월간 누적 데이터 조회 및 분석<br>  
6. 프로그램 종료

# 🚀 [버전별 개발 일지 & AI 협업 기록]

## 🟦 [1차 과제: V1.0] 시나리오 기획
    
### **🤖 AI 파트너십 과정**
<br>
1. 내용 1: '투두 메이트' 컨셉 도출<br> 
*프롬프트 요약: "파이썬으로 대학생들이 실생활에서 흥미를 느끼며 코딩할 수 있는 독창적인 프로젝트 주제 추천해 줘."<br>  
*적용 내용: AI가 제안한 여러 아이디어 중 **'갓생 살기: 투두 메이트'**라는 컨셉을 채택함. 단순히 할 일을 나열하는 기능을 넘어, 나의 하루 성취도를 데이터로 확인하고 '갓생'을 인증한다는 목표를 세우니 기획과 구현 과정이 훨씬 몰입도 있게 느껴짐.<br><br>
2. 내용 2: 로드맵 도출<br>  
*프롬프트 요약: "1차 과제 필수 요건(변수 5개, 자료형 3개 이상)을 충족하기 위한 투두 메이트 데이터 구조 모델링 논의"<br>  
*적용 내용: 단순히 '할 일' 개수만 받는 대신, 데이터 특성에 맞춰 **문자열(str - 이름, 날짜), 정수형(int - 총 개수, 완료 개수), 실수형(float - 목표 집중 시간)**으로 변수를 명확히 세분화하여 입력 데이터의 구조적 완성도를 높임.<br><br>
**증빙 자료<br> 
[1차 AI협업캡처.pdf 첨부 완료]

## 🟩 [2차 과제: V1.0] 입출력 + 리스트 + 조건문 - 향후 작성 예정
### **✨2차 과제 내용:**
 * 이번 과제는 성취도 정밀 진단 및 등급 부여 시스템 구현으로, 사용자로부터 데이터를 입력받아 리스트에 저장하고, 가중치 연산을 통해 결과값을 도출하는 시스템을 구축하는 것입니다. 특히 if-elif 조건문과 and/or 논리 연산자를 활용하여 성취도에 맞는 등급과 특별 칭호를 부여하는 지능형 로직을 완성해야 합니다.
  * input()으로 사용자의 이름, 할 일 완료 개수, 집중 시간, 목표 달성 수를 입력받아 리스트에 저장합니다.
  * 가중치 공식을 적용하여 종합 성취 점수 산출: (할 일 완료 개수 * 0.7) + (집중 시간 * 0.5) + (목표 달성 수 * 1.8).
  * if-elif-else를 사용하여 S등급부터 F등급까지 5단계 등급 판정 로직을 추가합니다.
  * and 연산자를 활용하여 '전설의 갓생러' 등 특별 칭호 부여 기능을 구현합니다.
    
### **🤖 AI 파트너십 과정**
1. **내용 1: 성취도 계산 로직 및 등급 도출**
* **프롬프트 요약:** "대학생 갓생 살기 컨셉에 맞춰서, 할 일 개수와 집중 시간을 조합해 성취 점수를 계산하는 재미있는 로직을 짜줘. 1학년 수준의 리스트와 조건문을 활용하고 싶어."   
* **적용 내용:** AI가 제안한 '가중치 점수 산출(수행률과 집중 시간 반영)' 방식과 **'점수가 높으면서 특정 조건을 만족하면 갓생 전문가 칭호 부여'**라는 복합 조건 아이디어를 코드에 반영함.   

2. **내용 2: 효율적인 리스트 활용 및 로드맵**
**프롬프트 요약:** "리스트, 조건문, 반복문을 배운 단계에서 이 프로그램을 어떻게 확장하면 좋을지 4단계 로드맵을 제안해줘."   
**적용 내용:** '기초 정보 입력 및 리스트 저장(1차) - 성취 등급 판독기 구현(2차) - 무한 메뉴 시스템 구축(3차) - 딕셔너리를 활용한 다중 데이터 관리(4차)'로 이어지는 확장 계획을 확립함.
    
### **🛠️ Troubleshooting & 기술 회고:**
1. **문제 1: 문은 줄 바꿈 또는 세미콜론으로 구분해야 합니다 (PylanceError)**
   * **원인:** 코드 중간에 적은 설명글(예: 5. 제어구조) 앞에 주석 기호인 #을 붙이지 않아서, 파이썬이 이 한글 문장을 실행해야 할 코드로 잘못 인식해 발생했습니다.
   * **해결:** 모든 한글 설명 줄 앞에 #을 추가하여 오류를 해결했습니다.
2. **문제 2: 문자열 리터럴이 종료되지 않았습니다 (SyntaxError)**
   * **원인:** print() 함수를 사용할 때 따옴표(')를 열고 닫지 않았거나, f-string(f'...')을 쓰면서 중괄호({}) 짝을 맞추지 않아 문자열이 끝나는 지점을 시스템이 찾지 못해 발생했습니다.
   * **해결:** 따옴표와 중괄호가 항상 짝을 이루어 닫혔는지 코드를 하나하나 검토하고, 변수명이 들어가는 중괄호 형식을 정확히 수정하여 해결했습니다.   
     
### **📁 증빙 자료:**
  * [2차_AI협업캡처.pdf 첨부 완료] 
  * [2차과제_실행결과.png]
<br>

## 🟨 [3차 과제: V3.0] 무한 루프와 메뉴 시스템 (반복문) - 향후 작성 예정

✨ **3차 과제 업데이트 내용:**
* **`while True` 기반 무한 루프 메뉴 도입:** 프로그램을 종료(`4번`)하기 전까지 무한히 반복 실행되며 사용자 입력을 대기하는 메인 메뉴 시스템을 구축했습니다.
* **기능별 독창적 함수 분리 (`def` 활용):** 기존 2차 과제 기능을 `input_todo()`, `show_todos()`, `complete_todo()`의 최소 3개 이상의 개별 함수로 쪼개어 가독성을 높였습니다.
* **매개변수 및 반환값(return) 패턴 적용:** 조회 함수에는 리스트를 인자로 전달하고, 완료 처리 함수에서는 처리 결과를 성공/실패 여부에 따라 `1` 또는 `0`으로 반환받도록 설계했습니다.
* **`global` 변수를 통한 상태 관리:** 프로그램 전체에서 유지되어야 하는 누적 완료 개수(`completed_count`)를 `global` 키워드를 활용해 함수 내부에서 제어 및 업데이트했습니다.

🤖 **AI 파트너십 과정**
1. **투두메이트 아키텍처 및 함수 분리 설계**
* **프롬프트 요약:** "기존에 작성하던 투두메이트 프로그램에 무한 루프 메뉴를 적용하고, 기능을 최소 3개의 독립된 함수로 분리해줘. 이때 매개변수, return문, global 키워드를 각각 1회 이상 무조건 포함해야 하고 메인 코드는 간결해야 해."
* **적용 내용:** AI의 추천을 받아 전역 변수로 투두 리스트와 완료 카운트를 선언했습니다. 입력, 조회, 완료 처리를 완전한 독립 함수로 분리하여 메인 로직(`while`문 내부)에는 오직 깔끔한 조건문과 함수 호출만 남겨 '목차형 구조'를 완성했습니다.
2. **리턴값 기반의 예외 및 상태 제어 최적화**
* **프롬프트 요약:** "complete_todo 함수에서 유저가 번호를 잘못 입력하거나 리스트가 빌 때의 예외 처리를 return문 조건에 맞춰서 보완해줘."
* **적용 내용:** 입력값이 범위를 벗어나거나 빈 값일 때 `return 0`을 반환하고, 정상적으로 할 일이 완료되었을 때만 완료 카운트를 올리며 `return 1`을 반환하도록 분기 처리를 구조화했습니다.

🛠️ **Troubleshooting & 기술 회고:**
1. **문제 1: 함수 내부에서 전역 변수(Count) 수정 시 발생하는 UnboundLocalError**
* **원인:** `complete_todo()` 함수 내에서 전역 변수인 `completed_count` 값을 직접 수정(`+= 1`)하려 시도했으나, 파이썬이 이를 지역 변수로 오인하여 할당 전에 참조되었다는 에러가 발생했습니다.
* **해결:** 함수 도입부에 `global completed_count` 키워드를 명시적으로 선언함으로써, 해당 변수가 지역 변수가 아닌 프로그램 최상단의 전역 변수임을 컴퓨터에게 명확히 알려주어 문제를 해결했습니다.
2. **문제 2: 메인 로직 가독성 저하 및 감점 요인 제거**
* **원인:** 처음 구조를 잡을 때 `while`문 내부에 메뉴 출력문과 입력값 조건 처리가 길게 나열되어, '메인 로직은 함수 호출 위주로 간결하게 구성할 것'이라는 과제 제약 사항에 위배될 위험이 있었습니다.
* **해결:** 사용자 메뉴 출력과 호출 부분을 제외한 모든 세부 연산 및 출력 가이드 라인을 각각의 함수(`input_todo`, `show_todos`, `complete_todo`) 내부로 전부 밀어 넣어 메인 코드를 목차처럼 직관적으로 리팩토링했습니다.

### **📁 증빙 자료:**
  * [3차_AI협업캡처.pdf 첨부 완료] (첨부 후 링크)
  * [3차과제_실행결과.jpg]
<br>

### 🟥 [4차 과제: V4.0] 모듈화 및 데이터 확장 (배열과 함수) - 🌟최종 완성 -- 향후 작성 예정
### **✨4차 과제 업데이트 내용:**
  * 내용.
    
### **🤖 AI 파트너십 과정**
 1. **내용 1**
    * **프롬프트 요약:**  ... 
    * **적용 내용:** ....
    
### **🛠️ Troubleshooting & 기술 회고:**
  1. **문제 1:** ...
     * **원인:** ...
     * **해결:** ..
     
### **📁 증빙 자료:**
  * [4차_AI협업캡처.pdf 첨부 완료] (첨부 후 링크)
  * [4차과제_실행결과.jpg]
<br>
